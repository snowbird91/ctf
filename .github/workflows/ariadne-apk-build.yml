name: Build Ariadne solver APK

on:
  push:
    branches:
      - chatgpt/ariadne-apk-fix-20260905
    paths:
      - .github/workflows/ariadne-apk-build.yml
      - tools/ariadne_ci_project.zip.part*
  workflow_dispatch:

permissions:
  contents: write

env:
  APK_B64_PATH: tools/ariadne_solver_v2_READY.apk.b64
  APK_SHA_PATH: tools/ariadne_solver_v2_READY.sha256

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: chatgpt/ariadne-apk-fix-20260905
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Install Android SDK packages
        shell: bash
        run: |
          set -euxo pipefail
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Reconstruct Android project
        shell: bash
        run: |
          set -euxo pipefail
          rm -rf /tmp/ariadne-project /tmp/ariadne-project.zip
          mkdir -p /tmp/ariadne-project
          cat tools/ariadne_ci_project.zip.part* > /tmp/ariadne-project.zip
          unzip -t /tmp/ariadne-project.zip
          unzip -q /tmp/ariadne-project.zip -d /tmp/ariadne-project
          find /tmp/ariadne-project -maxdepth 8 -type f -print

      - name: Build and verify signed debug APK
        shell: bash
        run: |
          set -euxo pipefail
          gradle --no-daemon -p /tmp/ariadne-project :app:assembleDebug --stacktrace
          APK=/tmp/ariadne-project/app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          unzip -t "$APK"
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs "$APK"
          base64 -w0 "$APK" > "$APK_B64_PATH"
          sha256sum "$APK" | sed 's#  .*#  ariadne_solver_v2_READY.apk#' > "$APK_SHA_PATH"
          wc -c "$APK" "$APK_B64_PATH"
          cat "$APK_SHA_PATH"

      - name: Persist encoded APK to branch
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add "$APK_B64_PATH" "$APK_SHA_PATH"
          if git diff --cached --quiet; then
            echo 'No APK output change.'
          else
            git commit -m 'Build Ariadne solver APK [skip ci]'
            git push origin HEAD:chatgpt/ariadne-apk-fix-20260905
          fi

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ariadne-solver-v2
          path: |
            /tmp/ariadne-project/app/build/outputs/apk/debug/app-debug.apk
            tools/ariadne_solver_v2_READY.sha256
          retention-days: 1
